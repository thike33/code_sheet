<%= link_to bookmark_path(current_user.bookmarks.find_by(post_id: post.id)), id: "unbookmark-button-for-post-#{post.id}", class: 'inline-block', data: { turbo_method: :delete } do %>
  <%= image_tag 'bookmark.png', class: 'w-6' %>
<% end %>